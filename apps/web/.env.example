# Leave empty when Vite proxies /api and /auth to the API (same hostname)
# VITE_API_URL=

# Production: UI and API on different subdomains
# VITE_API_URL=https://sis-api.skylarbarnes.com

# Local dev proxy target (default: http://localhost:3010)
# VITE_API_PROXY_TARGET=http://localhost:3010
